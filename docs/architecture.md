# Architecture

`go-proxy` is organized as a core/go consumer module under `go/`.

Packages provide proxy state management, miner and pool protocol handling, splitters, API routes, and append-only access/share logging.
