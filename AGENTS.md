# go-proxy Agent Notes

This repository uses the canonical core/go consumer layout.

- Go module: `go/`
- Workspace: `go.work`
- Local dependency mirrors: `external/`

Do not edit `.core/` runtime configuration or source inside `external/`.
